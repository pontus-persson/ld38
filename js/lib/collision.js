"use strict";
var Collision = function() {
    var self = this;

    this.collide = function(shape1, shape2) {
        
    }

    this.isColliding = function(shape1, shape2) {
        logsec(typeof shape1, typeof shape2);
    }
}